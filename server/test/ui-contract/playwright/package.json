{
  "name": "mbfd-ui-contract-harness",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated Playwright harness for the enterprise operator console UI/UX + accessibility contract tests. Does NOT modify the production package.json.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
